server:
  port: 96

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/university
    username: dba
    password: dba2021
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      jdbc:
        lob:
            non-contextual-creational: true